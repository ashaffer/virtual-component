{
  "name": "virtual-component",
  "description": "Components for virtual-dom",
  "repository": "git://github.com/ashaffer/virtual-component.git",
  "version": "0.2.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {
    "get-uid": "^1.0.1",
    "shallow-equals": "0.0.0"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-tape-runner": "^1.2.0",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
